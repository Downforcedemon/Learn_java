public class Store {

  String productType; 
  int inventoryCount;
  double inventoryPrice;

  // Constructor
  public Store(String product, int count, double price) {
    productType = product;
    inventoryCount = count;
    inventoryPrice = price;
  }

  // Overriding the toString() method
  @Override
  public String toString() {
    return "Store selling " + productType + " with inventory count: " + inventoryCount + " and price: " + inventoryPrice;
  }

  // main method
  public static void main(String[] args) {
    System.out.println("Start of the main method.");

    // Create the instance
    Store cookieShop = new Store("cookies", 12, 2.50); // Example arguments
    System.out.println(cookieShop);

    System.out.println("End of the main method."); 
  } 
}

// pseudocode for the above
// A. Define the Store Class with some attributes
// B. declare instance variables such productType, inventoryCount
// C. define constructor with parameters such as product, count with types
// D. override
// E. main method --> print message --> create instance(create store object with parameters) --> print details of the store (cookieshop) 

